// Compatibility entry point: all current examples share the requested-output validator.
// The adjacent pre-refresh JSON remains historical, not evidence for the new media.
import { main } from '../verify.mjs';
await main();
